---
status: closed
type: grilling
hitl: true
assignee: pi
blocked-by: []
closed: 2026-08-07
---

# 任务列表收敛与筛选交互细节

## Question

在「runtime 筛选 + 默认收敛」已定的前提下，定死以下细节：

- 默认收敛窗口：按天数还是条数？running / error 任务是否永远可见？
- 筛选控件形态与位置：chips、分段还是下拉？与创建区 runtime 分段的关系是什么（是否联动）？
- 「显示更多」的交互：原位展开、分页还是跳转历史视图？展开后能否再收起？
- 标题展示规则：原始首消息截断之外，是否需要回退规则（如首消息是 XML/URL 时显示什么）？
- 空态：无任务、筛选无结果两种状态各显示什么？

## Resolution（2026-08-07，pi 定案，用户授权收官任务书）

- 默认收敛：7 天窗口保留；running/error 永远可见（既有逻辑，确认保留）。
- 筛选控件：**不加**。sidebar 纯展示为 2026-08-06 定案（runtime/Agent 切换在对话框下方），
  runtime 筛选与之冲突，否决；本 ticket 原前提「runtime 筛选已定」被新定案取代。
- 显示更多：原位展开 + 可收起（既有「显示更多（N）/收起历史」），保留。
- 标题回退：首消息为标记（`<` 开头）或纯 URL 时回退第一条纯文本行，再没有则「新任务」；
  实现 `task-title.ts` + 单测。混合内容保留原文截断 42。
- 空态：无任务「还没有任务」；无筛选结果态随筛选否决不成立。
