---
status: superseded
type: grilling
hitl: true
assignee:
blocked-by: [0006]
---

# 设置顶层重组与现有 tab 迁移映射

已被 2026-08-06 定案取代：Agent Project/Evals 退役，Agent 面板不再存在，设置重组前提失效。

## Question

在「设置顶层 = 通用 / Pi / Codex / Agent / Browser」已定的前提下，定死：

- 顶层条目顺序与命名；入口是否仍留在侧边栏 footer。
- 现有 tab 的迁移映射：Providers/模型 → Pi；Skills/Extensions → Pi 还是 Agent；
  Evals → Agent 面板内还是暂留独立；Browser 配对 → Browser；外观/工作区/默认 runtime → 通用。
- Codex 面板第一版内容：只放 sandbox/approval 的说明与可见状态，还是允许配置？
- Agent 面板与「Agent Project」概念的关系（依赖 Agent 定位 ticket 的结论）。
